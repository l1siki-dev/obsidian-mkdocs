- **Line Endings:** JSON is sensitive to formatting. Marking it as `text` in `.gitattributes` allows Git to normalize line endings (LF vs CRLF) across different operating systems (Windows vs Mac/Linux), preventing "phantom" changes where the whole file looks modified just because of a newline character.